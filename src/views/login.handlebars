<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Iniciar Sesión</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .login-container {
            max-width: 360px;
            margin: auto;
            margin-top: 64px;
            text-align: center;
        }

        h1 {
            font-weight: bold;
        }

        h2 {
            color: #555;
        }

        form {
            margin-top: 32px;
        }

        .input-group {
            width: 100%;
            margin-bottom: 32px;
        }

        .input-group-text {
            background-color: #F8A126;
            color: #fff;
            border: none;
        }

        .form-control {
            border: 1px solid #ddd;
            border-radius: 4px;
            padding: 10px;
        }

        .btn-container {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .btn-secondary,
        .btn-warning {
            flex: 1;
            margin-right: 10px;
            padding: 10px;
            border-radius: 4px;
            cursor: pointer;
        }

        .btn-secondary {
            background-color: #6c757d;
            color: #fff;
        }

        .btn-warning {
            background-color: #ffc107;
            color: #212529;
        }

        .btn-secondary:hover,
        .btn-warning:hover {
            filter: brightness(90%);
            /* Agrega un efecto de brillo al pasar el ratón sobre los botones */
        }

        .btn-success {
            background-color: #28a745;
            color: #fff;
            border: none;
            padding: 10px;
            border-radius: 4px;
            cursor: pointer;
        }

        .btn-success:hover {
            background-color: #218838;
        }

        a {
            text-decoration: none;
            color: #007BFF;
        }
    </style>
</head>

<body>
    <div class="login-container">
        <h1><b>CODERStore</b></h1>
        <h2>Login de usuario</h2>

        <form action="/api/login" method="post">
            <div class="input-group">
                <span class="input-group-text" id="group1"><i class="far fa-envelope"></i></span>
                <input type="email" name="email" class="form-control" placeholder="email" required="required">
            </div>

            <div class="input-group">
                <span class="input-group-text" id="group2"><i class="fas fa-lock"></i></span>
                <input type="password" name="password" class="form-control" required="required" placeholder="Password">
            </div>

            <div class="btn-container">
                <a href="/api/github" class="btn btn-secondary">
                    <i class="fab fa-github"></i> Ingresar con Github
                </a>
                <a href="/api/google" class="btn btn-warning">
                    <i class="fab fa-google"></i> Ingresar con Google
                </a>
            </div>

            <div style="margin-bottom: 32px;">
                <a href="/restore">¿Olvidaste tu contraseña?</a>
            </div>

            <button type="submit" class="btn btn-success">Ingresar</button>
        </form>

        <p style="margin-top: 16px;">
            <a href="/register"><i class="fas fa-user-plus"></i> Registrarse</a>
        </p>
    </div>

    <!-- Agrega la referencia a Font Awesome -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
</body>

</html>
<!DOCTYPE html>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Iniciar Sesión</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .login-container {
            max-width: 360px;
            margin: auto;
            margin-top: 64px;
        }

        h1 {
            font-weight: bold;
        }

        h2 {
            color: #555;
        }

        form {
            margin-top: 32px;
        }

        .input-group {
            width: 300px;
            margin-bottom: 32px;
        }

        .input-group-text {
            background-color: #F8A126;
            color: #fff;
            border: none;
        }

        .form-control {
            border: 1px solid #ddd;
            border-radius: 4px;
            padding: 10px;
        }

        .btn-success {
            background-color: #28a745;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
        }

        .btn-success:hover {
            background-color: #218838;
        }

        a {
            text-decoration: none;
            color: #007BFF;
        }
    </style>
</head>

<body>
    <div class="login-container">
        <h1><b>CODERStore</b></h1>
        <h2>Login de usuario</h2>

        <form action="/api/login" method="post">
            <div class="input-group">
                <span class="input-group-text" id="group1"><i class="fas fa-user"></i></span>
                <input type="email" name="username" class="form-control" placeholder="Nombre" required="required">
            </div>

            <div class="input-group">
                <span class="input-group-text" id="group2"><i class="fas fa-lock"></i></span>
                <input type="password" name="password" class="form-control" required="required" placeholder="Password">
            </div>

            <div style="margin-bottom: 32px;">
                <a href="/api/github">Ingresar con Github</a>
            </div>

            <div style="margin-bottom: 32px;">
                <a href="/restore">¿Olvidaste tu contraseña?</a>
            </div>

            <button type="submit" class="btn btn-success">Ingresar</button>
        </form>

        <p style="margin-top: 16px;">
            <a href="/register">Registrarse</a>
        </p>
    </div>

    <!-- Agrega la referencia a Font Awesome -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
</body>

</html>